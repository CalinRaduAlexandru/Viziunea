// Canonical profile/member service. The legacy filename remains as a compatibility
// adapter for older imports while the application moves to the profiles model.
export { listMembers, updateMember, demoMembers } from './member-repository.js';
